export const createSideNavCategoryOptionTag = (value, text) => {
  console.log("value and label", value, text);
  //   const container = getSideNavTagContainer();
  //   const template = getSideNavTagTemplate();

  //   setSideNavTagWrapper();
};

export const deleteSideNavCategoryOptionTag = (value, text) => {};
